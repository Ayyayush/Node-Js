/**
 * ! Backend with Node.js
 *
 * ! What is Backend & Client-Server Architecture
 *
 * Client-Server architecture ek model hai jahan par:
 * - Client request bhejta hai
 * - Server us request ko process karke response bhejta hai
 *
 * Example:
 * Browser (client) → Request → Server → Response → Browser
 *
 *
 * ! What is Server (Backend)
 *
 * Server ek aisa computer/system hota hai jo:
 * - Resources, data, services aur programs provide karta hai client ko over a network
 * - Client se request leta hai
 * - Us request ko process karta hai
 * - Phir response wapas bhejta hai
 *
 * Example:
 * - JSON Placeholder API
 *
 *
 * ! Important Note
 *
 * - Hum frontend (browser JavaScript) se APIs ya endpoints create nahi kar sakte
 * - Frontend sirf APIs ko consume karta hai
 * - Node.js ka use karke hum backend bana sakte hain
 * - Node.js HTTP server create karta hai
 *
 *
 * ! What is Client (Frontend)
 *
 * Client wo hota hai jo:
 * - Server ko request bhejta hai
 * - Response receive karta hai
 *
 * Examples:
 * - Browser (Chrome, Edge)
 * - Mobile Apps
 *
 *
 * ! What is Node.js Actually
 *
 * - Node.js ek C++ based application hai
 * - Yeh JavaScript ko browser ke bahar run karne deta hai
 * - Yeh ek runtime environment provide karta hai
 * - Chrome ke V8 JavaScript engine par built hai
 *
 * Simple words mein:
 * → Node.js = JavaScript ko server par run karne ka environment
 *
 *
 * ! What is Node.js Used For
 *
 * - Web servers aur APIs banana
 * - CLI tools banana
 *   Example:
 *   → TypeScript
 *   → Webpack
 *   → ESLint
 *   → Babel
 * - IoT (Internet of Things) applications
 *
 *
 * ! Node.js ke Uses
 *
 * - Backend development (APIs banana)
 * - Real-time applications (chat apps, live updates)
 * - File handling
 * - Streaming applications
 * - CLI tools banana
 *
 *
 * ! Important Note
 *
 * - Jab hum index.html ko Live Server se open karte hain,
 *   toh wo internally Node.js ka use karta hai
 *
 * - Live Server browser ke andar nahi,
 *   VS Code ke andar Node.js environment mein run hota hai
 *
 * - Rule yaad rakho:
 *   Agar JavaScript browser ke bahar run ho rahi hai,
 *   toh wo Node.js mein run ho rahi hai
 *
 */